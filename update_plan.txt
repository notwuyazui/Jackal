2026.9.8
    未来本项目的主要更新方向：
    1. 优化项目代码结构，使之更易读、更易维护、可扩展性更强。
        （1）拆分jackal_env中的Reward、Observation、Action、Render等模块。
        （2）重构统一的游戏引擎BattleEngine，将部分目前env承担的任务下放到engine层面完成。
        （3）支持更多算法
    2. 支持多任务多智能体强化学习MOMARL训练，与传统SMAC作出区别。
    3. 将游戏引擎的运行部署在GPU上实现加速：
        （1）长期向考虑接入Isaac Sim，或短期内可将状态、奖励等tensor化。
        （2）支持并行化训练。
    4. 替换视觉元素，丰富游戏玩法。

2026.9.11
    短期内优化项目结构：
    1. 统一冷却、射程和弹药覆盖参数，修复潜在行为错误。
    2. 添加 BattleWorld，先包装现有三个 Manager，不立即重写实体。
    3. 将开火、视野、射线检测和单位创建迁入游戏层。
    4. 引入 WorldSnapshot、CombatEvent，让观测和奖励不再直接访问整个 JackalEnv。
    5. 使用空间索引和每帧感知缓存。
    6. 最后解耦 Pygame 渲染，并重构训练入口。